# Load packages.
library(tidyverse)
library(tidymodels)
# Import soup data and select the Sales, Any_Disp_Spend, Any_Feat_Spend, and Any_Price_Decr_Spend variables
set.seed(7)
soup_data <- soup_data |>
mutate(
nonsense_01 = rnorm(nrow(soup_data), mean = 0, sd = 5),
nonsense_02 = rnorm(nrow(soup_data), mean = 50, sd = 10),
nonsense_03 = rnorm(nrow(soup_data), mean = -10, sd = 20),
nonsense_04 = rnorm(nrow(soup_data), mean = 100, sd = 100),
nonsense_05 = runif(nrow(soup_data), min = -5, max = 5),
nonsense_06 = runif(nrow(soup_data), min = 25, max = 500),
nonsense_07 = runif(nrow(soup_data), min = -50, max = 10),
nonsense_08 = rbinom(nrow(soup_data), size = 1, prob = 0.25),
nonsense_09 = rbinom(nrow(soup_data), size = 3, prob = 0.10),
nonsense_10 = rbinom(nrow(soup_data), size = 7, prob = 0.85)
)
soup_data <- read_csv("soup_data.csv")
soup_data <- soup_data |> select(Sales, Any_Disp_Spend, Any_Feat_Spend, Any_Price_Decr_Spend)
set.seed(7)
soup_data <- soup_data |>
mutate(
nonsense_01 = rnorm(nrow(soup_data), mean = 0, sd = 5),
nonsense_02 = rnorm(nrow(soup_data), mean = 50, sd = 10),
nonsense_03 = rnorm(nrow(soup_data), mean = -10, sd = 20),
nonsense_04 = rnorm(nrow(soup_data), mean = 100, sd = 100),
nonsense_05 = runif(nrow(soup_data), min = -5, max = 5),
nonsense_06 = runif(nrow(soup_data), min = 25, max = 500),
nonsense_07 = runif(nrow(soup_data), min = -50, max = 10),
nonsense_08 = rbinom(nrow(soup_data), size = 1, prob = 0.25),
nonsense_09 = rbinom(nrow(soup_data), size = 3, prob = 0.10),
nonsense_10 = rbinom(nrow(soup_data), size = 7, prob = 0.85)
)
# Set the randomization seed.
set.seed(42)
# Split the data.
soup_split <- initial_split(soup_data, prop = 0.90)
soup_split
# Access the training data.
training(soup_split)
# Access the testing data.
testing(soup_split)
# Fit the simple model with training data.
fit_simple <- linear_reg() |>
set_engine("lm") |>
fit(
Sales ~ Any_Disp_Spend + Any_Feat_Spend + Any_Price_Decr_Spend,
data = training(soup_split)
)
# Fit the complex model with training data
fit_complex <- linear_reg() |>
set_engine("lm") |>
fit(
Sales ~ .,
data = training(soup_split)
)
# Compare overall model fit.
bind_rows(
glance(fit_simple),
glance(fit_complex)
)
# Compute predictive fit for simple model
fit_simple |>
predict(new_data = testing(soup_split)) |>
bind_cols(testing(soup_split)) |>
rmse(truth = Sales, estimate = .pred)
# compute predictive fit for complex model
fit_complex |>
predict(new_data = testing(soup_split)) |>
bind_cols(testing(soup_split)) |>
rmse(truth = Sales, estimate = .pred)
# Compute predictive fit for the simple model on the training data (BAD)
fit_simple |>
predict(new_data = training(soup_split)) |>
bind_cols(training(soup_split)) |>
rmse(truth = Sales, estimate = .pred)
# Compute predictive fit for the complex model on the training data (BAD)
fit_complex |>
predict(new_data = training(soup_split)) |>
bind_cols(training(soup_split)) |>
rmse(truth = Sales, estimate = .pred)
ifood <- read_csv("ifood.csv")
ifood <- ifood |>
mutate(total_spend = MntWines + MntFruits + MntMeatProducts + MntFishProducts + MntSweetProducts + MntGoldProds)
# Load packages.
library(tidyverse)
library(tidymodels)
# Import soup data and select the Sales, Any_Disp_Spend, Any_Feat_Spend, and Any_Price_Decr_Spend variables
soup_data <- read_csv("soup_data.csv") |>
select(Sales, Any_Disp_Spend, Any_Feat_Spend, Any_Price_Decr_Spend)
set.seed(7)
soup_data <- soup_data |>
mutate(
nonsense_01 = rnorm(nrow(soup_data), mean = 0, sd = 5),
nonsense_02 = rnorm(nrow(soup_data), mean = 50, sd = 10),
nonsense_03 = rnorm(nrow(soup_data), mean = -10, sd = 20),
nonsense_04 = rnorm(nrow(soup_data), mean = 100, sd = 100),
nonsense_05 = runif(nrow(soup_data), min = -5, max = 5),
nonsense_06 = runif(nrow(soup_data), min = 25, max = 500),
nonsense_07 = runif(nrow(soup_data), min = -50, max = 10),
nonsense_08 = rbinom(nrow(soup_data), size = 1, prob = 0.25),
nonsense_09 = rbinom(nrow(soup_data), size = 3, prob = 0.10),
nonsense_10 = rbinom(nrow(soup_data), size = 7, prob = 0.85)
)
# Set the randomization seed.
set.seed(42)
# Split the data.
soup_split <- initial_split(soup_data, prop = 0.90)
soup_split
# Access the training data.
training(soup_split)
# Access the testing data.
testing(soup_split)
# Fit the simple model with training data.
fit_simple <- linear_reg() |>
set_engine("lm") |>
fit(
Sales ~ Any_Disp_Spend + Any_Feat_Spend + Any_Price_Decr_Spend,
data = training(soup_split)
)
# Fit the complex model with training data
fit_complex <- linear_reg() |>
set_engine("lm") |>
fit(
Sales ~ .,
data = training(soup_split)
)
# Compare overall model fit.
bind_rows(
glance(fit_simple),
glance(fit_complex)
)
fit_simple |>
predict(new_data = testing(soup_split))
testing(soup_split)
fit_simple |>
predict(new_data = testing(soup_split)) |>
bind_cols(testing(soup_split))
# Compute predictive fit for simple model
fit_simple |>
predict(new_data = testing(soup_split)) |>
bind_cols(testing(soup_split)) |>
rmse(truth = Sales, estimate = .pred)
# compute predictive fit for complex model
fit_complex |>
predict(new_data = testing(soup_split)) |>
bind_cols(testing(soup_split)) |>
rmse(truth = Sales, estimate = .pred)
# Compute predictive fit for the simple model on the training data (BAD)
fit_simple |>
predict(new_data = training(soup_split)) |>
bind_cols(training(soup_split)) |>
rmse(truth = Sales, estimate = .pred)
# Compute predictive fit for the complex model on the training data (BAD)
fit_complex |>
predict(new_data = training(soup_split)) |>
bind_cols(training(soup_split)) |>
rmse(truth = Sales, estimate = .pred)
# Compute predictive fit for the complex model on the training data (BAD)
fit_complex |>
predict(new_data = training(soup_split)) |>
bind_cols(training(soup_split)) |>
mae(truth = Sales, estimate = .pred)
ifood <- read_csv("ifood.csv")
View(ifood)
ifood_split <- initial_split(ifood, prop = 0.95)
View(ifood)
ifood <- ifood |>
mutate(total_spend = MntWines + MntFruits + MntMeatProducts + MntFishProducts + MntSweetProducts + MntGoldProds)
# Fit the simple model with training data.
fit_simple <- linear_reg() |>
set_engine("lm") |>
fit(
total_spend ~ income + recency,
data = training(ifood_split)
)
ifood <- read_csv("ifood.csv")
ifood <- ifood |>
mutate(total_spend = MntWines + MntFruits + MntMeatProducts + MntFishProducts + MntSweetProducts + MntGoldProds)
ifood_split <- initial_split(ifood, prop = 0.95)
# Fit the simple model with training data.
fit_simple <- linear_reg() |>
set_engine("lm") |>
fit(
total_spend ~ income + recency,
data = training(ifood_split)
)
# Fit the simple model with training data.
fit_simple <- linear_reg() |>
set_engine("lm") |>
fit(
total_spend ~ Income + Recency,
data = training(ifood_split)
)
View(ifood)
fit_complex <- linear_reg() |>
set_engine("lm") |>
fit(
total_spend ~ Income + Recency + Year_Birth + Kidhome + Teenhome,
data = training(ifood_split)
)
# Compute predictive fit for simple model
fit_simple |>
predict(new_data = testing(ifood_split)) |>
bind_cols(testing(ifood_split)) |>
rmse(truth = total_spend, estimate = .pred)
# Compute predictive fit for simple model
fit_simple |>
predict(new_data = testing(ifood_split)) |>
bind_cols(testing(ifood_split)) |>
mae(truth = total_spend, estimate = .pred)
# Compute predictive fit for simple model
fit_complex |>
predict(new_data = testing(ifood_split)) |>
bind_cols(testing(ifood_split)) |>
mae(truth = total_spend, estimate = .pred)
fit_complex |>
tidy(conf.int = TRUE)
set.seed(1)
ifood_split <- initial_split(ifood, prop = 0.95)
# Fit the simple model with training data.
fit_simple <- linear_reg() |>
set_engine("lm") |>
fit(
total_spend ~ Income + Recency,
data = training(ifood_split)
)
fit_complex <- linear_reg() |>
set_engine("lm") |>
fit(
total_spend ~ Income + Recency + Year_Birth + Kidhome + Teenhome,
data = training(ifood_split)
)
# Compute predictive fit for simple model
fit_simple |>
predict(new_data = testing(ifood_split)) |>
bind_cols(testing(ifood_split)) |>
mae(truth = total_spend, estimate = .pred)
# Compute predictive fit for simple model
fit_complex |>
predict(new_data = testing(ifood_split)) |>
bind_cols(testing(ifood_split)) |>
mae(truth = total_spend, estimate = .pred)
# Load packages.
library(tidyverse)
library(tidymodels)
# Import soup data and select the Sales, Any_Disp_Spend, Any_Feat_Spend, and Any_Price_Decr_Spend variables
soup_data <- read_csv("soup_data.csv") |>
select(Sales, Any_Disp_Spend, Any_Feat_Spend, Any_Price_Decr_Spend)
set.seed(7)
soup_data <- soup_data |>
mutate(
nonsense_01 = rnorm(nrow(soup_data), mean = 0, sd = 5),
nonsense_02 = rnorm(nrow(soup_data), mean = 50, sd = 10),
nonsense_03 = rnorm(nrow(soup_data), mean = -10, sd = 20),
nonsense_04 = rnorm(nrow(soup_data), mean = 100, sd = 100),
nonsense_05 = runif(nrow(soup_data), min = -5, max = 5),
nonsense_06 = runif(nrow(soup_data), min = 25, max = 500),
nonsense_07 = runif(nrow(soup_data), min = -50, max = 10),
nonsense_08 = rbinom(nrow(soup_data), size = 1, prob = 0.25),
nonsense_09 = rbinom(nrow(soup_data), size = 3, prob = 0.10),
nonsense_10 = rbinom(nrow(soup_data), size = 7, prob = 0.85)
)
# Set the randomization seed.
set.seed(42)
# Split the data.
soup_split <- initial_split(soup_data, prop = 0.90)
soup_split
# Access the training data.
training(soup_split)
# Access the testing data.
testing(soup_split)
# Fit the simple model with training data.
fit_simple <- linear_reg() |>
set_engine("lm") |>
fit(
Sales ~ Any_Disp_Spend + Any_Feat_Spend + Any_Price_Decr_Spend,
data = training(soup_split)
)
# Fit the complex model with training data
fit_complex <- linear_reg() |>
set_engine("lm") |>
fit(
Sales ~ .,
data = training(soup_split)
)
# Compare overall model fit.
bind_rows(
glance(fit_simple),
glance(fit_complex)
)
fit_simple |>
predict(new_data = testing(soup_split))
fit_simple |>
predict(new_data = testing(soup_split)) |>
bind_cols(testing(soup_split))
# Compute predictive fit for simple model
fit_simple |>
predict(new_data = testing(soup_split)) |>
bind_cols(testing(soup_split)) |>
rmse(truth = Sales, estimate = .pred)
# Compute predictive fit for simple model
fit_simple |>
predict(new_data = testing(soup_split)) |>
bind_cols(testing(soup_split)) |>
mae(truth = Sales, estimate = .pred)
testing(soup_split)
# Compute predictive fit for simple model
fit_simple |>
predict(new_data = testing(soup_split)) |>
bind_cols(testing(soup_split)) |>
mae(truth = Sales, estimate = .pred)
# compute predictive fit for complex model
fit_complex |>
predict(new_data = testing(soup_split)) |>
bind_cols(testing(soup_split)) |>
mae(truth = Sales, estimate = .pred)
View(soup_data)
# Compute predictive fit for the simple model on the training data (BAD)
fit_simple |>
predict(new_data = training(soup_split)) |>
bind_cols(training(soup_split)) |>
rmse(truth = Sales, estimate = .pred)
# Compute predictive fit for the complex model on the training data (BAD)
fit_complex |>
predict(new_data = training(soup_split)) |>
bind_cols(training(soup_split)) |>
mae(truth = Sales, estimate = .pred)
# Compute predictive fit for the simple model on the training data (BAD)
fit_simple |>
predict(new_data = training(soup_split)) |>
bind_cols(training(soup_split)) |>
mae(truth = Sales, estimate = .pred)
# Compute predictive fit for the simple model on the training data (BAD)
fit_simple |>
predict(new_data = training(soup_split)) |>
bind_cols(training(soup_split)) |>
rmse(truth = Sales, estimate = .pred)
# Compute predictive fit for the complex model on the training data (BAD)
fit_complex |>
predict(new_data = training(soup_split)) |>
bind_cols(training(soup_split)) |>
rmse(truth = Sales, estimate = .pred)
ifood <- read_csv("ifood.csv")
ifood <- ifood |>
mutate(total_spend = MntWines + MntFruits + MntMeatProducts + MntFishProducts + MntSweetProducts + MntGoldProds)
View(ifood)
set.seed(916)
ifood_split <- initial_split(ifood, prop = 0.90)
# Fit the simple model with training data.
fit_carson <- linear_reg() |>
set_engine("lm") |>
fit(
total_spend ~ Income + Recency + Year_Birth,
data = training(ifood_split)
)
# Fit the simple model with training data.
fit_carson <- linear_reg() |>
set_engine("lm") |>
fit(
total_spend ~ Income + Recency + Year_Birth,
data = training(ifood_split)
)
# Fit the complex model with training data
fit_complex <- linear_reg() |>
set_engine("lm") |>
fit(
Sales ~ Income + Recency + Year_Birth + NumWebVisitsMonth + Complain + Kidhome + Teenhome,
data = training(ifood_split)
)
# Fit the simple model with training data.
fit_carson <- linear_reg() |>
set_engine("lm") |>
fit(
total_spend ~ Income + Recency + Year_Birth,
data = training(ifood_split)
)
# Fit the complex model with training data
fit_complex <- linear_reg() |>
set_engine("lm") |>
fit(
total_spend ~ Income + Recency + Year_Birth + NumWebVisitsMonth + Complain + Kidhome + Teenhome,
data = training(ifood_split)
)
# Compute predictive fit for simple model
fit_carson |>
predict(new_data = testing(ifood_split)) |>
bind_cols(testing(ifood_split)) |>
mae(truth = total_sales, estimate = .pred)
# Compute predictive fit for simple model
fit_carson |>
predict(new_data = testing(ifood_split)) |>
bind_cols(testing(ifood_split)) |>
mae(truth = total_spend, estimate = .pred)
# Compute predictive fit for simple model
fit_complex |>
predict(new_data = testing(ifood_split)) |>
bind_cols(testing(ifood_split)) |>
mae(truth = total_spend, estimate = .pred)
fit_complex |>
tidy(conf.int = TRUE)
